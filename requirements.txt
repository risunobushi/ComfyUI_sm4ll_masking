torch>=1.13
torchvision>=0.14
transformers>=4.20.0
Pillow>=9.0.0
opencv-python>=4.5.0
numpy>=1.20.0
easyocr>=1.4.1
# User needs to have comfyui_controlnet_aux (or another DensePose provider) installed separately
# User also needs to have Detectron2 installed if their chosen DensePose provider relies on it.
